import { responsiveSize } from '../../../utils/dimensions';

export default {
  loader: {
    flexDirection: 'row',
    alignSelf: 'center',
    height: responsiveSize(26),
    marginTop: responsiveSize(26),
  },
};
