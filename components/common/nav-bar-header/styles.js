import { responsiveSize } from '../../../utils/dimensions';

export default {
  header: {
    height: responsiveSize(60),
    flexDirection: 'row',
    width: '100%',
    paddingHorizontal: '5%',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
};
