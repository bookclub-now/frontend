import { responsiveSize } from '../../../utils/dimensions';

export default {
  popup: {
    width: responsiveSize(280),
    height: responsiveSize(152),
    bottom: '2%',
    position: 'absolute',
    alignSelf: 'center',
  },
};
